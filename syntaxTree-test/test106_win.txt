package main

	func factorial (n int) (int, int) { //vic return parametru
	dex_n := n - 1
	if n < 2 {
		return 1
	} else {
		tmp := 0
		tmp = factorial(dec_n)
		return n * tmp
func factorial (n int) (int, int) { //vic return parametru
	dex_n := n - 1
	if n < 2 {
		return 1
	} else {
		tmp := 0
		tmp = factorial(dec_n)
		return n * tmp
	}
	}